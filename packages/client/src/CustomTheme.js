import { theme as rimbleTheme } from "rimble-ui";

const CustomTheme = {
  ...rimbleTheme,
  colors: {
    ...rimbleTheme.colors,
    primary: "#4E174F",
    background: "#fffff8",
    pink: "#D2B1B5"
  }
};

export default CustomTheme;
